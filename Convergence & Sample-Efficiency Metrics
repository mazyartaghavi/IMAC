import numpy as np

def convergence_speed(reward_history, threshold=0.95):
    """
    Measures iterations to reach a fraction of asymptotic reward.
    """
    max_reward = np.max(reward_history)
    for i, r in enumerate(reward_history):
        if r >= threshold * max_reward:
            return i
    return len(reward_history)
